
<h2><%= @pet.name %></h2>

  <%= link_to 'Edit Project', edit_pet_path(@pet) %>
  <%= link_to 'Delete Project', pet_path(@pet), method: :delete,
                                                      data: { confirm:
                                                               "Are you sure you want to delete this project?"
                                                            } %>



<%= link_to "New Pet", new_pet_path %>
